{% extends "baseUsuario.html.twig" %}
{% block title %}{{ usuario.nombre }} {{ usuario.apellidos }}{% endblock %}

{% block content %}
    <div id="datosUsuario" class="active">
        {% embed "datos.html.twig" with {'usuario': usuario} %}
        {% endembed %}
    </div>
    <div id="objetosUsuario" style="display: none">
        {% embed "objetos.html.twig" with {'objetos': usuario.objetos} %}
        {% endembed %}
    </div>
    <div id="favoritosUsuario" style="display: none">
        {% embed "favoritos.html.twig" with {'favoritos': usuario.favoritos} %}
        {% endembed %}
    </div>
    <div id="preferenciasUsuario" style="display: none">
        {% embed "preferencias.html.twig" with {'preferencias': usuario.preferencias} %}
        {% endembed %}
    </div>
    <div id="reservasUsuario" style="display: none">
        {% embed "reservas.html.twig" with {'reservas': usuario.reservas} %}
        {% endembed %}
    </div>
{% endblock %}